import { UpAnddownDateDirective } from './up-anddown-date.directive';

describe('UpAnddownDateDirective', () => {
  it('should create an instance', () => {
    const directive = new UpAnddownDateDirective();
    expect(directive).toBeTruthy();
  });
});
